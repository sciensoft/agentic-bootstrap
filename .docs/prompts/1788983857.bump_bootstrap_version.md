# Request

Bump the bootstrap-version header to `2026-09-09` in preparation for merging the context-management improvement branch (Cuts 1–3 + MCP + Migration). Promote the `[Unreleased]` block in `CHANGELOG.md` to a dated `[2026-09-09]` entry with a short summary, and start a fresh empty `[Unreleased]` above per the Format note at the bottom of the changelog.

## Reasoning

The branch `feat/context-cut-1-trigger-index` carries five commits' worth of context-management overhaul plus prior unreleased UX / interview polish. The user is preparing to push and merge, so the version marker needs to advance so re-runs on existing bootstrapped projects trigger the upgrade narrative (Step 8 report), and so the lint invariant that `bootstrap-version` matches the most recent dated `CHANGELOG` entry stays green after the branch merges to `develop`.

## Consulted rules

- `workflow.md` — one prompt file per task, this cut is the version bump.
- `workflow-changes.md` — `CHANGELOG.md` is a user-visible surface and moves in the same commit.
- `best-practices.md` (Markdown-content discipline) — CHANGELOG's Keep-a-Changelog format is followed; the version marker at the top of `AGENTIC-BOOTSTRAP.md` matches the promoted entry's date (lint check #4).

`workflow-security.md`, `workflow-testing.md`, `workflow-metrics.md` not applicable (no security surface, no test/metrics code touched — the change is a two-line date bump plus a CHANGELOG heading rename).

## Output

- `AGENTIC-BOOTSTRAP.md` header: `<!-- bootstrap-version: 2026-06-12 -->` → `<!-- bootstrap-version: 2026-09-09 -->`.
- `CHANGELOG.md`: `[Unreleased]` renamed to `[2026-09-09] — Context-management overhaul (trigger index, enforcement, proactive rules, MCP, migration) + interview + UX polish`; new empty `[Unreleased]` opened above.
- Lint green (5/5 checks); `check_version` now compares `2026-09-09` header against `2026-09-09` most-recent dated entry — matches.
- Two small gaps from the coverage audit remain open as follow-up work: `rules-check` should exit non-zero on findings (§9.2 of the improvements doc), and no verification scaffolding was emitted for the Claude `PreToolUse` hook script (§9.1). Neither blocks the merge; both are separate cuts if the user wants them closed.
