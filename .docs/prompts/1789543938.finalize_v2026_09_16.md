# Request

Fold the two landing-page fix-ups (`321ca49` header stack fix; `067258d` tree trims + banner scroll + 10× copy) into the `[Unreleased]` landing-page bullet, promote `[Unreleased]` to `[2026-09-16]` per the Keep-a-Changelog Format note, add a fresh empty `[Unreleased]` above, and bump the `bootstrap-version` header in `AGENTIC-BOOTSTRAP.md` to match. Prepares the branch for merge.

## Reasoning

I forgot the CHANGELOG on the two fix-up commits — user asked, and the answer is honestly "no, only the initial landing-page commit updated it." The two fix-ups are the same landing-page task's refinements, so they belong as sub-bullets under the existing `[Unreleased]` entry rather than as separate entries.

Version bump follows the same pattern as `ba16749` (the earlier `[2026-09-09]` promotion): when a coherent release is ready to merge, promote `[Unreleased]` to a dated entry and update the header so `check_version` (lint check #4) stays green after merge. The current `[Unreleased]` batches together the doctor-mode hook-verification Check #11 (`f15e5e5`) and the landing-page rebuild + refinements — one coherent release.

## Consulted rules

- `workflow.md` — new artifact-producing task; prompt file + commit + push.
- `workflow-changes.md` — `CHANGELOG.md` is user-visible; version marker in `AGENTIC-BOOTSTRAP.md` (also visible to re-run flows) moves in the same commit.
- `best-practices.md` (Markdown-content discipline) — CHANGELOG's Keep-a-Changelog format preserved; version marker matches promoted date (lint invariant #4).

Not applicable: `workflow-security.md`, `workflow-metrics.md`, `workflow-testing.md`.

## Output

Same feature branch (`feat/context-cut-1-trigger-index`), one commit for the finalization.

- `CHANGELOG.md`:
  - `[Unreleased]` landing-page bullet gains three sub-bullets covering the header-stack fix (`321ca49`) and the tree-trim + banner-scroll + 10×-lighter-copy refinements (`067258d`).
  - `[Unreleased]` renamed to `[2026-09-16] — Landing-page rebuild (context-management ad, subscription banner, per-agent tabs) + Doctor-mode hook-reinforcement check`.
  - Fresh empty `[Unreleased]` opened above.
- `AGENTIC-BOOTSTRAP.md`:
  - `<!-- bootstrap-version: 2026-09-09 -->` → `<!-- bootstrap-version: 2026-09-16 -->`.
- Lint green (`check_version` now compares `2026-09-16` header against `2026-09-16` most-recent dated entry).

Branch ready to merge after this commit — 12 commits total.

## Refinements

- **2026-09-16 fix-up on `a8980dd`**: the version bump caught the `bootstrap-version` header in `AGENTIC-BOOTSTRAP.md` but missed the hard-coded version badge on the landing page — `docs/index.html` line 1811 still read `v2026-06-10`. The badge is the visible "Changelog" pill in the *Adoption* section's Star / Changelog / Contribute row. Updated to `v2026-09-16` to match the bootstrap-version header. No other stale version references on the page (grep confirmed).
