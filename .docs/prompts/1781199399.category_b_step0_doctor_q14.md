# Request

> Let's focus on "Category B" and "Honourable mention — one more Q to compress" for now!

(Picked up after the remind-pending-items recap. Targets the four Category B passes and the Q14 honourable mention identified earlier.)

## Reasoning

Three of the four originally-proposed Category B passes are bundled into this single PR; the fourth (posture *Use when / Do not use when* boilerplate dedup) was dropped after a grep confirmed the trailers only appear once in the file — there was no actual duplication to dedup, just one substantial warning section at the end of the `.claude/settings.json` posture variants. Keeping that one section as-is preserves the safety prose for the riskiest posture choice (`BYPASS`).

The three passes that did ship share a common shape: **prose → structured layout, same content**. None changes behaviour. None changes a flag value or a dispatch rule. The wins are readability for the maintainer and lower token cost for naive readers (the smart-agent path was already paying ~25k thanks to the offset-read Index).

Q14 LICENSE was the honourable-mention Q-compression candidate from the same conversation — at 568 chars it's the smallest of the wall-of-text questions but it carries enough trade-off discussion (when to pick which license) that surfacing a *Fits when* column is genuine added value, not just symmetry with Q2 / Q3 / Q5.

## Output

- [`AGENTIC-BOOTSTRAP.md`](../../AGENTIC-BOOTSTRAP.md):
  - **Step 0 (Part 1)** — the *Legacy-layout migration* megabullet at the top of Detect-run-mode is restructured. One parent bullet introducing the trigger + the question, followed by three sub-bullets covering the three branches (`Migrate` / `Leave` / `Either way`). Cuts the bullet from ~280 words to ~140 while preserving every branch and every command.
  - **Doctor mode (Part 1)** — the 10-item numbered prose checklist is converted to a 10-row table with columns `# / Check / What to flag`. Each row is one line; "What to flag" captures the failure mode without the verbose qualifier prose. Severity grouping in the report-shape block below the table is unchanged.
  - **Q14 LICENSE (Part 2)** — same question + sub-table pattern as the prior Q-compressions. Question text keeps the inline 4-option list for chat-verbatim agents; new `#### License options — for Q14` sub-table adds a *Fits when* column with concrete guidance per slot.
  - **Template Index** — 78 rows refreshed to absorb line shifts from the three passes.
- [`CHANGELOG.md`](../../CHANGELOG.md) `[Unreleased]` — new entry under *Changed*.
- This prompt file.

## What was dropped (pass 2)

The original five-pass Category B proposal listed a *posture dedup* pass — extracting the `Use only when / Do not use when` warning from the BYPASS variants of each tool (`.claude` / `.cursor` / `.codex` / `.windsurf`) into a single shared section. A grep showed those trailers appear **once**, only on the `.claude/settings.json` BYPASS variant. The other three tools' BYPASS variants are much shorter and don't share the prose. No actual duplication to dedup — pass dropped.

(The remaining originally-proposed pass — the recurrent *"This section…"* preamble trim across ~80 Part 4 templates — is a separate fiddly pass deferred to a future PR.)
