# Request

> Let's do it!

(In response to a proposal — *Category B, pass #1* of the editorial-tightening plan — to compress Q5's 700-word single-cell paragraph into a short question + a small **Architecture options** sub-table.)

## Reasoning

Q5 was the worst offender in the interview: one massive line of inline option descriptions that the user reads as a wall of text and the agent loads as ~700 words of context just to ask one question. The seven architecture shapes are easily expressed as a small table with `Slot` · `Shape` · `Fits when` columns — same content, an order of magnitude more skimmable. The agent gets a structured source it can render as a picker, a flow, or quote inline; the user gets to scan a 7-row table instead of parsing prose.

No behavior changes:

- Same seven canonical shapes.
- Same `ARCH` flag value set.
- Same `LAYERED = (ARCH ≠ FLAT)` derivation.
- The existing **Q5 disambiguation** sub-section for system-topology answers (serverless / monorepo / SOA / etc.) and vocabulary aliases (bare *DDD* / *clean code* / etc.) is unchanged — Q5's new question text just references it explicitly.

This is the first of five passes in Category B. The four remaining: Step 0 legacy-migration megabullet, posture "use-when" boilerplate dedup, Doctor mode 10-item checklist tightening, recurrent preamble trim. Each ships independently.

## Output

- [`AGENTIC-BOOTSTRAP.md`](../../AGENTIC-BOOTSTRAP.md) Part 2 Project-identity tier — Q5 row compressed from ~1,900 chars / one massive paragraph to ~600 chars referencing the new sub-table. A new `#### Architecture options — for Q5` sub-section inserted between the interview table and the existing `#### Q5 disambiguation` sub-section.
- [`CHANGELOG.md`](../../CHANGELOG.md) `[Unreleased]` — new entry under *Changed*.
- This prompt file.

## Known follow-up

- **Template Index offsets.** The Architecture options sub-table adds ~16 lines to Part 2, which shifts every Part 4 template offset in the Template Index by +16. The drift safeguard (re-grep when an offset doesn't land) keeps smart-agent reads correct, but a quick offset refresh in the same merge wave is worth doing. Captured implicitly by [`.docs/todos/lint-template-index-offsets.md`](../todos/lint-template-index-offsets.md) — once the lint check from that TODO exists, CI will catch this kind of drift automatically.
