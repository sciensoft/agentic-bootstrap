# Request

> Remove the "Contribute" from the top-nav

## Reasoning

The desktop top-nav carried six items; removing "Contribute" frees a slot for higher-priority navigation as the page grows. The call-to-action still exists — the page surfaces Contribute via the mobile menu, the in-page anchor (`#contribute`), and the footer link — so the removal is purely a desktop top-nav slimming.

## Output

- [`docs/index.html:225`](../../docs/index.html#L225): removed the `<a href="#contribute">Contribute</a>` link from the desktop top-nav.
- Mobile menu, section anchor (line 960), and footer link (line 1022) untouched.
- Committed as `df32fc8`.
