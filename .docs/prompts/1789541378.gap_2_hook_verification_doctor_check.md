# Request

Close Gap 2 from the context-management improvements review: add an agent-agnostic hook-reinforcement smoke test to doctor mode as Check #11. Per the user's direction, this rides on the same feature branch (`feat/context-cut-1-trigger-index`) as a new commit.

## Reasoning

Cuts 1–3 + MCP + Migration shipped, plus the version bump and `rules-check` removal. The one outstanding item was Gap 2 — verifying that emitted hook-shaped reinforcements actually fire on the user's host. The user rejected an emitted test script (nobody would run it), rejected `make rules-check` (same problem), and pointed at doctor mode as the compliance path users actually reach for. Folding hook verification into doctor mode aligns with the "one downloadable file" delivery model: no external scripts, no user Makefile touched — just prose in the bootstrap that instructs the running agent to run the check.

The check is described agent-agnostically. For hosts with a runtime hook primitive (Claude Code's `PreToolUse`, Cursor's `alwaysApply` rules), the agent verifies the hook is functional (not just present). For hosts without one (Aider, Codex, Continue, Windsurf, Copilot), the check silently skips — those hosts rely on Layer 1 (`AGENTS.md` trigger index, attentional) and Layer 2 (`.pre-commit-config.yaml` + `scripts/check_consulted_rules.sh`, at commit) enforcement paths which don't need a runtime smoke test.

The check catches the failure mode Cut 2's mechanical layer trades for: silent no-ops. If `jq` isn't installed, if the case ladder doesn't match the project's actual layout, if there's a bash version quirk on WSL — the hook exits with no output and no error. Claude Code proceeds without the reminder, no error surfaces, the mechanical enforcement layer silently degrades. Re-running the bootstrap can't fix these (they're environment issues, not template bugs). Doctor mode's smoke test is where they surface.

## Consulted rules

- `workflow.md` — new artifact-producing task; prompt file + commit + push.
- `workflow-changes.md` — `CHANGELOG.md` (user-visible surface) moves in the same commit.
- `best-practices.md` (Markdown-content discipline) — table row addition, heading hierarchy stable.

Not applicable: `workflow-security.md` (no security surface touched), `workflow-metrics.md` / `workflow-testing.md` (proactive-discipline rules, but no code paths added — prose-only cut), `workflow-todos.md` (no deferrals captured or removed).

## Output

Same feature branch, one commit per cut per user cadence.

- `AGENTIC-BOOTSTRAP.md`:
  - Doctor-mode checklist (Part 1 Step 0) gains row #11: **Hook-reinforcement smoke test**. Per-host verification described agent-agnostically — Claude pipes synthesized JSON into the hook script and checks stdout; Cursor parses the `.mdc` frontmatter and body; other hosts silent-skip. Zero writes — respects doctor mode's hard rule.
  - Doctor-mode report shape's `Drift` subsection parenthetical expanded to name hook-reinforcement hygiene.
- `CHANGELOG.md` `[Unreleased]` gains an `Added` bullet describing the new check and why it exists (silent-fail failure mode of runtime hooks that re-bootstrap can't fix).
- No self-application to this repo — no `.claude/hooks/` or `.cursor/rules/trigger-index.mdc` present here (`AGENTS_USED=[CLAUDE]` but no runtime hooks configured); the check silently skips on this project.
- No `bootstrap-version` bump — this is a same-day incremental addition to the `[2026-09-09]` release. The user can either promote `[Unreleased]` to a new dated entry before merge if they consider this substantial, or roll it into the next version bump.

Pile after this: empty. Branch is ready for merge.
