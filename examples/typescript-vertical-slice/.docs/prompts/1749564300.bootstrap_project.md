# Request

Follow `AGENTIC-BOOTSTRAP.md` to bootstrap this repo.

# Reasoning

First-time run (no `.agents/rules/workflow.md` sentinel). Interview answers in `.agents/bootstrap.json`:

- `PROJECT_NAME=helix`, purpose: "Issue and verify ephemeral access tokens for a mesh of internal services."
- `AGENTS_USED=[CURSOR, AIDER]` → `.cursor/rules/agents.mdc` + `.aider.conf.yml` adapters written. No `CLAUDE.md` or `.claude/settings.json` (Claude not in set; `POSTURE=N_A`).
- `LANG=TypeScript/Node` → `package.json`, `tsconfig.json`, `eslint.config.js` + `.prettierrc.json`, `Makefile` (TS variant), `tests/` scaffold.
- `ARCH=VERTICAL_SLICE` → `.agents/rules/layered-architecture.md` (Vertical Slice variant); `LAYERED=true`.
- `WEB=true`, `LLM=false`, `ENV_VARS=true` → `.env.example` + web sub-sections of `methodology.md`.
- `TESTING=true` → `.agents/rules/workflow-testing.md`.
- `LICENSE=MIT`, holder "Helix Platform Team".

Step 4b web-search probe succeeded; `best-practices.md` was refined against current TypeScript + Fastify + `jose` sources (then later trimmed back to the stub in this example folder for compactness — the marker comment retains the refinement-was-attempted record).

# Output

Files written per the Part 3 matrix:

- `AGENTS.md` (Sacred — primary brief)
- `.cursor/rules/agents.mdc`, `.aider.conf.yml` (per-tool adapters; both Sacred / Mixed per matrix)
- `.agents/rules/{workflow,workflow-todos,workflow-security,workflow-testing,best-practices,layered-architecture}.md` (Canon)
- `.agents/bootstrap.json` (Canon — persisted answers)
- `.docs/adrs/{README,0000-adr-template}.md` (Mixed / Canon)
- `.docs/todos/README.md` (Canon)
- `.docs/security/methodology.md` (Canon — web sub-sections enabled)
- `package.json`, `tsconfig.json`, `vitest.config.ts`, `eslint.config.js`, `.prettierrc.json`, `Makefile`, `.gitignore`, `.env.example`, `.editorconfig`, `.gitattributes`, `.pre-commit-config.yaml`
- `README.md`, `LICENSE` (MIT), `SECURITY.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Sacred — `CONTRIB=true`)
- This prompt file (`.docs/prompts/1749564300.bootstrap_project.md`)

First commit: `Bootstrap project with agentic workflow conventions`.
