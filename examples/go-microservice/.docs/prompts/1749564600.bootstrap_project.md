# Request

Follow `AGENTIC-BOOTSTRAP.md` to bootstrap this repo.

# Reasoning

First-time run (no `.agents/rules/workflow.md` sentinel). Interview answers in `.agents/bootstrap.json`:

- `PROJECT_NAME=quill`, purpose: "Accept order events from the storefront and forward fulfilment requests to the warehouse."
- `AGENTS_USED=[CLAUDE, COPILOT]` → `CLAUDE.md` + `.claude/settings.json` + `.github/copilot-instructions.md` adapters written. No Cursor / Aider files.
- `POSTURE=TRUSTED_DEV` → safe git + Go (`go:*`) commands pre-allowed.
- `LANG=Go` → `go.mod`, `Makefile` (Go variant), `.golangci.yml`, `tests/` scaffold.
- `ARCH=MICROSERVICE` → `.agents/rules/layered-architecture.md` (Microservice variant — 4-Layer DDD internals + cross-service surface); `LAYERED=true`.
- `WEB=true`, `LLM=false`, `ENV_VARS=true` → `.env.example`; web sub-sections of `methodology.md` enabled.
- `METRICS=true` → `.agents/rules/workflow-metrics.md` written.
- `TESTING=true` → `.agents/rules/workflow-testing.md` written.
- `LICENSE=MIT`, holder "Quill Order Flow Team".

Step 4b web-search probe failed (no network in this run), so `best-practices.md` was written as the **stub variant** with the `<!-- best-practices: stub -->` marker.

# Output

Files written per the Part 3 matrix:

- `AGENTS.md` (Sacred — primary brief)
- `CLAUDE.md`, `.claude/settings.json` (Sacred / Mixed — `TRUSTED_DEV` posture, Go addendum)
- `.github/copilot-instructions.md` (Canon — Copilot adapter)
- `.agents/rules/{workflow,workflow-todos,workflow-security,workflow-testing,workflow-metrics,best-practices,layered-architecture}.md` (Canon)
- `.agents/bootstrap.json` (Canon — persisted answers)
- `.docs/adrs/{README,0000-adr-template}.md` (Mixed / Canon)
- `.docs/todos/README.md` (Canon)
- `.docs/security/methodology.md` (Canon — web sub-sections enabled)
- `go.mod`, `Makefile`, `.golangci.yml`, `.gitignore`, `.env.example`, `.editorconfig`, `.gitattributes`, `.pre-commit-config.yaml`, `Dockerfile`, deploy manifest
- `README.md`, `LICENSE` (MIT), `SECURITY.md`, `CHANGELOG.md` (Sacred)
- This prompt file (`.docs/prompts/1749564600.bootstrap_project.md`)

First commit: `Bootstrap project with agentic workflow conventions`.

Step 8 report flagged `best-practices.md` as stubbed; per-agent enablement matrix in the file shows how to enable refinement next time (Claude Code: add `WebSearch` / `WebFetch` to `permissions.allow`).
