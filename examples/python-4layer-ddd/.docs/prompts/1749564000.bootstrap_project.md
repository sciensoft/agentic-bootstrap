# Request

Follow `AGENTIC_BOOTSTRAP.md` to bootstrap this repo.

# Reasoning

Per the bootstrap's Part 1 playbook, this is a **first-time** run (no `.agents/rules/workflow.md` sentinel exists). Interview answers captured (see `.agents/bootstrap.json`):

- `PROJECT_NAME=orbital`, one-line purpose: "Track and analyse satellite orbital decay events."
- `AGENTS_USED=[CLAUDE]` → `CLAUDE.md` adapter + `.claude/settings.json` will be written.
- `POSTURE=TRUSTED_DEV` → safe git + Python (`uv:*`, `pytest`, `ruff`) commands pre-allowed.
- `LANG=Python` → `pyproject.toml` + `tests/` + `ruff.toml` + `Makefile` (Python variant) get written.
- `ARCH=4_LAYER_DDD` → `.agents/rules/layered-architecture.md` (4-Layer DDD variant) gets written; `LAYERED=true`.
- `WEB=true`, `LLM=false`, `ENV_VARS=true` → `.env.example` written; `methodology.md` web sub-sections enabled.
- `TESTING=true` → `.agents/rules/workflow-testing.md` written; rule wired into `AGENTS.md` + `CLAUDE.md`.
- `LICENSE=MIT`, holder "Orbital Mechanics Inc."

Step 4b web-search probe failed (host without network in this environment), so `best-practices.md` was written as the **stub variant** with the `<!-- best-practices: stub -->` marker. The Step 8 report will surface this and offer the per-agent enablement matrix.

# Output

Files written (Canon / Mixed / Sacred per the matrix):

- `AGENTS.md`, `CLAUDE.md` (Sacred — user-owned after first write)
- `.agents/rules/{workflow,workflow-todos,workflow-security,workflow-testing,best-practices,layered-architecture}.md` (Canon)
- `.agents/bootstrap.json` (Canon — persisted interview answers)
- `.claude/settings.json` (Mixed — `TRUSTED_DEV` posture, Python addendum)
- `.docs/adrs/{README.md,0000-adr-template.md}` (Mixed / Canon)
- `.docs/todos/README.md` (Canon)
- `.docs/security/methodology.md` (Canon — web sub-sections enabled, LLM sub-sections skipped)
- `pyproject.toml`, `ruff.toml`, `Makefile`, `.gitignore`, `.env.example`, `.editorconfig`, `.gitattributes`, `.pre-commit-config.yaml`, `tests/` scaffold (Mixed / Sacred per matrix)
- `README.md`, `LICENSE` (MIT), `SECURITY.md`, `CHANGELOG.md` (Sacred)
- This prompt file (`.docs/prompts/1749564000.bootstrap_project.md`)

First commit message: `Bootstrap project with agentic workflow conventions`.

Step 8 report flagged `best-practices.md` as stubbed (no web access during this run); the per-agent enablement matrix in the file shows how to enable refinement next time.
