<!-- This is a stub. The real bootstrap file produced this example. -->
<!-- See the canonical AGENTIC-BOOTSTRAP.md two levels up: ../../AGENTIC-BOOTSTRAP.md -->

# Agentic Bootstrap (stub)

This example was produced by handing the canonical [`AGENTIC-BOOTSTRAP.md`](../../AGENTIC-BOOTSTRAP.md) to an agent and answering the interview as recorded in [`.agents/bootstrap.json`](./.agents/bootstrap.json).

The file lives here as a placeholder so the repository is self-identifying as bootstrapped — re-runs and the `bootstrap-doctor` audit will both look for this marker. The actual content is intentionally not duplicated; consult `../../AGENTIC-BOOTSTRAP.md` for the playbook.
